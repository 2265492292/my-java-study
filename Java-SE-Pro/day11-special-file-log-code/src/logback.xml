<configuration debug="false">
    <!-- 一、日志文件存放的地址,可以更改 -->
    <!--    <property name="LOG_HOME" value="D:/kk"/>-->
    <!-- 这里更改 -->
    <!-- 二、日志输出到控制台的代码 -->
    <!--    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">-->
    <!--CONSOLE ：表示当前欸之信息是可以输出到控制台的-->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <!--        输出流对象 默认 System.out 改为 System.err -->
        <target>System.out</target>
        <!-- 刷新时间 -->
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <!--格式化输出：%d表示时间，%thread表示线程名，%c:日志对象的名字，%-5level：级别从左显示5个字符宽度
            %msg:日志消息 %n：换行符-->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.sss} [%-5level] %c [%thread]:%msg%n</pattern>
            <!--            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} - %msg%n</pattern>-->
        </encoder>
    </appender>
    <!-- 三、日志输出到本地文件的代码 -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!--日志输出路径-->
        <file>E:/log/itheima-data.log</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!--<fileNamePattern>${LOG_HOME}/log.%d{yyyy-MM-dd}.%i.log</fileNamePattern>-->
            <!--%i：新建文件索引-->
            <!--通过指定压缩文件名称，来确定分割文件方式-->
            <fileNamePattern>E:/log/itheima-data-%i-%d{yyyy-MM-dd}-.log.gz</fileNamePattern>
            <!-- 当文件到达多大就自动新建一个文件继续存放数据 -->
            <!--            文件拆分大小-->
            <maxFileSize>10MB</maxFileSize>
            <!--日志文件保留天数-->
            <MaxHistory>60</MaxHistory>
        </rollingPolicy>
        <!-- 刷新时间 -->
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} - [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>utf-8</charset>
        </encoder>
    </appender>
    <!-- 四、总控制开关的代码 -->
    <root level="all">
        <!-- 输出级别（多少），默认就是DEBUG。TRACE<DEBUG<INFO<WARN<ERROR -->
        <appender-ref ref="CONSOLE"/>
        <!-- 输出到控制台，不想输出到控制台可以删掉 -->
        <appender-ref ref="FILE"/>
        <!-- 输出本地磁盘文件夹中，不想输出到文件夹中可以删掉 -->
    </root>
</configuration>